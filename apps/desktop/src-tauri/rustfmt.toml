# 기본값 100은 이 코드베이스에 좁다 — 정책 판단 코드가 서술적인 식별자와 한국어 주석을 쓰므로
# 100자에서는 한 표현이 여러 줄로 쪼개져 오히려 읽기 어려워진다. 120으로 둔다.
#
# rustfmt는 대상 파일에서 상위 디렉터리로 올라가며 이 파일을 찾으므로, src-tauri에 두면
# `core/`(별도 워크스페이스)에도 함께 적용된다.
max_width = 120

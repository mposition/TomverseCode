{
  "name": "tomverse-code",
  "version": "0.1.0",
  "private": true,
  "description": "Tomverse Code — AI coding assistant desktop app (Windows). See docs/design/ for architecture docs.",
  "workspaces": [
    "packages/*",
    "apps/*",
    "evals/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "typecheck": "npm run typecheck --workspaces",
    "test": "npm run test --workspace=@tomverse/sidecar && npm run test --workspace=@tomverse/hypothesis-gate",
    "//test:e2e": "실제 Rust 호스트 바이너리를 요구한다 — 아래 core:build를 먼저 실행할 것.",
    "test:e2e": "npm run test:e2e --workspace=@tomverse/sidecar",
    "core:build": "cargo build --manifest-path apps/desktop/src-tauri/core/Cargo.toml",
    "core:test": "cargo test --manifest-path apps/desktop/src-tauri/core/Cargo.toml",
    "//verify": "전체 검증. 순서가 중요하다: **build가 typecheck보다 먼저**여야 한다 — sidecar는 protocol의 빌드 산출물(dist)에 대해 타입 검사하므로, clean clone이나 fetch 직후처럼 dist가 없거나 낡은 상태에서 typecheck를 먼저 돌리면 잘못된 protocol 타입을 읽는다. core:build가 test:e2e 바로 앞인 이유는 e2e가 tomverse-host 바이너리를 요구하기 때문이다. `npm install`/`npm ci`는 사용자가 사전에 수행한다.",
    "verify": "npm run build && npm run typecheck && npm run test && npm run core:test && npm run core:build && npm run test:e2e",
    "//build": "`--if-present`를 쓰지 않는다: 워크스페이스가 build 스크립트를 잃으면 조용히 통과하는 대신 실패해야 한다.",
    "//typecheck": "같은 이유로 `--if-present` 없음. 타입 검사가 사라진 워크스페이스를 통과로 세지 않는다.",
    "//test": "테스트가 있는 워크스페이스를 명시한다 — `--if-present`로 뭉개지 않는다. 새 워크스페이스에 테스트를 추가하면 여기에도 넣을 것.",
    "//gate:g": "가설 게이트 G — evals/hypothesis-gate/README.md. gate:g:test는 실제 API를 부르지 않으므로 npm test에도 포함된다.",
    "gate:g:test": "npm run test --workspace=@tomverse/hypothesis-gate",
    "gate:g:validate": "npm run build --workspace=@tomverse/hypothesis-gate && node evals/hypothesis-gate/dist/src/cli.js validate",
    "gate:g:dry-run": "npm run build --workspace=@tomverse/hypothesis-gate && node evals/hypothesis-gate/dist/src/cli.js dry-run",
    "gate:g:pilot": "npm run build --workspace=@tomverse/hypothesis-gate && node evals/hypothesis-gate/dist/src/cli.js pilot",
    "gate:g:run": "npm run build --workspace=@tomverse/hypothesis-gate && node evals/hypothesis-gate/dist/src/cli.js run",
    "gate:g:report": "npm run build --workspace=@tomverse/hypothesis-gate && node evals/hypothesis-gate/dist/src/cli.js report"
  },
  "engines": {
    "node": ">=20"
  }
}

{
  "name": "@tomverse/sidecar",
  "version": "0.1.0",
  "private": true,
  "description": "Node sidecar: Orchestrator, Provider Adapters, Context Engine. See docs/design/process-architecture.md.",
  "type": "module",
  "main": "./dist/src/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/src/index.js",
    "//test": "이 Node 버전은 `node --test <디렉터리>`가 동작하지 않는다(CLAUDE.md 함정 기록) — 파일 경로를 직접 나열한다.",
    "test": "npm run build && node --test dist/test/boundary.test.js dist/test/cancellation.test.js dist/test/machine.test.js dist/test/triage.test.js dist/test/providers.test.js dist/test/router.test.js dist/test/context.test.js dist/test/planner.test.js dist/test/orchestrator.test.js",
    "//test:e2e": "실제 tomverse-host 바이너리를 요구한다: cargo build --manifest-path ../../apps/desktop/src-tauri/core/Cargo.toml",
    "test:e2e": "npm run build && node --test dist/test/e2e.test.js dist/test/e2e-persistence.test.js",
    "test:all": "npm run test && npm run test:e2e"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.68.0",
    "@tomverse/protocol": "*",
    "openai": "^6.9.0"
  },
  "devDependencies": {
    "@tomverse/toolchain": "*",
    "@types/node": "^24.9.0",
    "typescript": "^5.9.3"
  },
  "types": "./dist/src/index.d.ts",
  "//exports": "예산 ledger는 제품 코드다. 가설 게이트가 이걸 가져다 쓴다 — 측정 도구에만 두면 제품의 유료 호출 경로에는 같은 보호가 없게 된다.",
  "exports": {
    ".": "./dist/src/index.js",
    "./budget": {
      "types": "./dist/src/budget/ledger.d.ts",
      "default": "./dist/src/budget/ledger.js"
    },
    "./registry": {
      "types": "./dist/src/routing/registry.d.ts",
      "default": "./dist/src/routing/registry.js"
    }
  }
}

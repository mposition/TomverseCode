{
  "name": "@tomverse/sidecar",
  "version": "0.1.0",
  "private": true,
  "description": "Node sidecar: Orchestrator, Provider Adapters, Context Engine. See docs/design/process-architecture.md.",
  "type": "module",
  "main": "./dist/src/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/src/index.js",
    "//test": "이 Node 버전은 `node --test <디렉터리>`가 동작하지 않는다(CLAUDE.md 함정 기록) — 파일 경로를 직접 나열한다.",
    "test": "npm run build && node --test dist/test/boundary.test.js dist/test/machine.test.js dist/test/triage.test.js dist/test/providers.test.js dist/test/router.test.js dist/test/context.test.js dist/test/planner.test.js dist/test/orchestrator.test.js",
    "//test:e2e": "실제 tomverse-host 바이너리를 요구한다: cargo build --manifest-path ../../apps/desktop/src-tauri/core/Cargo.toml",
    "test:e2e": "npm run build && node --test dist/test/e2e.test.js",
    "test:all": "npm run test && npm run test:e2e"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.68.0",
    "@tomverse/protocol": "*",
    "openai": "^6.9.0"
  },
  "devDependencies": {
    "@types/node": "^24.9.0",
    "typescript": "^5.9.3"
  }
}
